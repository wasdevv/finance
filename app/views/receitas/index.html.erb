<table class="table">
  <thead>
    <tr>
      <th scope="col" class="text-center">Descrição</th>
      <th scope="col" class="text-center">Valor</th>
      <th scope="col" class="text-center"></th>
    </tr>
  </thead>
  <tbody>
    <% @receitas.each do |receita| %>
      <tr>
        <%# <th scope="row">1</th> %>
        <td class="align-middle text-center"><%= despesa.destricao %></td>
        <td class="align-middle text-center"><%= despesa.valor %></td>
        <td>
          <%= link_to 'Mostrar', receita %>
          <%= link_to 'Editar', edit_despesa_path(receita)%>
          <%= link_to 'Excluir', receita, method: :delete, data: { turbo_method: :delete } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Nova Despesa'%>